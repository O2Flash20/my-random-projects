import pyautogui
import keyboard

keyboard.add_abbreviation('$', 'this.')

keyboard.add_abbreviation('^', 'class NAME { \nconstructor(){\n')

keyboard.add_abbreviation('~', "for (let i = 0; i < 9; i++) { \n")

keyboard.add_abbreviation('vect', "createVector(")

keyboard.wait()
